# ===========================================
# Environment Variables Template
# ===========================================
# Copiez ce fichier vers .env.local pour le développement
# Les variables NEXT_PUBLIC_* sont exposées côté client
#
# IMPORTANT: Les environnements dev et prod utilisent des ressources AWS distinctes
# - Dev: ressources AWS avec préfixe 'dev_' (tables DynamoDB, Cognito pool, AppSync)
# - Prod: ressources AWS avec préfixe 'prod_' (déployées via Amplify)

# Application
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=development

# API Configuration
NEXT_PUBLIC_API_URL=https://dev-api.tma.example.com/api

# AWS Configuration
NEXT_PUBLIC_AWS_REGION=eu-west-1
NEXT_PUBLIC_AWS_USER_POOL_ID=eu-west-1_YourDevPoolId
NEXT_PUBLIC_AWS_USER_POOL_CLIENT_ID=your-dev-cognito-client-id
NEXT_PUBLIC_AWS_APPSYNC_ENDPOINT=https://your-dev-appsync.appsync-api.eu-west-1.amazonaws.com/graphql

# DynamoDB (AWS - pas de endpoint local)
DYNAMODB_TABLE_PREFIX=dev_

# Debug & Logging
NEXT_PUBLIC_DEBUG=true
LOG_LEVEL=debug

# Google Drive Integration
NEXT_PUBLIC_GOOGLE_DRIVE_ENABLED=false
GOOGLE_DRIVE_CLIENT_ID=your-google-client-id
GOOGLE_DRIVE_CLIENT_SECRET=your-google-client-secret
